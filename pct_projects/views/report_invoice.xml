<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--
        Customize invoice PDF to show Project Sequence Number instead of Invoice Number
        This affects both:
        - account.account_invoices (Invoice PDF with payments)
        - account.account_invoices_without_payment (PDF without Payment)

        Both use account.report_invoice_document as the base template.
    -->

    <!--
        Hide the invoice number in the document title header and show project_sequence_number instead
        Target: <span t-if="o.name and o.name != '/'" t-field="o.name">
    -->
    <template id="report_invoice_document_hide_invoice_number"
              inherit_id="account.report_invoice_document"
              priority="50">
        <!--
            Replace the entire layout_document_title block to show project_sequence_number
            instead of invoice number for customer invoices.
        -->
        <xpath expr="//div[hasclass('page')]/t[@t-set='layout_document_title']" position="replace">
            <t t-set="layout_document_title">
                <span t-if="not proforma"/>
                <span t-else="">PROFORMA</span>
                <span t-if="o.move_type == 'out_invoice' and o.state == 'posted'">Invoice</span>
                <span t-elif="o.move_type == 'out_invoice' and o.state == 'draft'">Draft Invoice</span>
                <span t-elif="o.move_type == 'out_invoice' and o.state == 'cancel'">Cancelled Invoice</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'posted'">Credit Note</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'draft'">Draft Credit Note</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'cancel'">Cancelled Credit Note</span>
                <span t-elif="o.move_type == 'in_refund'">Vendor Credit Note</span>
                <span t-elif="o.move_type == 'in_invoice'">Vendor Bill</span>
                <!-- Project Sequence Number for customer invoices -->
                <span t-if="o.project_sequence_number and o.move_type == 'out_invoice'"
                      t-field="o.project_sequence_number">PRJ/2024/01/00001</span>
                <!-- Fallback to invoice name for other cases -->
                <span t-if="(not o.project_sequence_number or o.move_type != 'out_invoice') and o.name and o.name != '/'"
                      t-field="o.name">INV/2023/0001</span>
            </t>
        </xpath>
    </template>

    <!--
        Also update the Payment Communication section to show project sequence number
    -->
    <template id="report_invoice_payment_ref_project_sequence"
              inherit_id="account.report_invoice_document"
              priority="50">
        <xpath expr="//p[@name='payment_communication']/span[@t-field='o.payment_reference']" position="replace">
            <!-- Show Project Sequence Number for customer invoices that have one -->
            <span t-if="o.project_sequence_number and o.move_type == 'out_invoice'"
                  class="fw-bold"
                  t-field="o.project_sequence_number">PRJ/2024/01/00001</span>
            <!-- Fallback to original payment reference for other document types -->
            <span t-if="not o.project_sequence_number or o.move_type != 'out_invoice'"
                  class="fw-bold"
                  t-field="o.payment_reference">INV/2023/00001</span>
        </xpath>
    </template>
</odoo>
